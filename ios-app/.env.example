# Copy to .env (never versioned).
# https address of the server the app talks to. The pages live inside the app; only the API is remote.
CARNET_API_BASE_URL=https://example.invalid
# Bundle identifier of the app (must be unique for your Apple account).
CARNET_APP_ID=app.carnetmalin.ios
