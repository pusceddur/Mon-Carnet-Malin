# Copy to .env (never versioned).
# https address of the server that hosts the web app.
CARNET_SERVER_URL=https://example.invalid
# Bundle identifier of the app (must be unique for your Apple account).
CARNET_APP_ID=app.carnetmalin.ipad
