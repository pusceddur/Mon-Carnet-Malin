// Links into the reader for other modules (exercises, notes).
// `/lire/:documentId?page=N&quote=…` — N is the page number shown to the child (pageIndex + 1); `quote` is highlighted briefly.
export { readerPath } from './model';
